-- set alpha transparency for counters
setCounterDragAlpha("0,5")

-- create the grid (a, b, xoff, yoff)
hexagonalGrid("37,6", "64,4", "0,0", "32,0")

-- specify the grid (radius, r, g ,b, a)
gridSpecify(5, "1,0", "0,0", "0,0", "0,5" )

-- show the grid
gridShow(true)
